@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Oink is a static library, so its private compression dependencies must be
# resolved by downstream consumers at link time.
@OINK_CONFIG_FIND_DEPS@
if(NOT TARGET oink::oink)
    include(${CMAKE_CURRENT_LIST_DIR}/oink-targets.cmake)
endif()
